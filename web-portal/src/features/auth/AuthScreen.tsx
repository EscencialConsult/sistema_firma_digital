/**
 * AuthScreen — legacy wrapper, replaced by AppRouter + AuthLayout.
 * Kept for reference only; no longer rendered by PortalShell.
 */
import { LoginPage } from "./LoginPage";

export function AuthScreen() {
  return <LoginPage />;
}
